table:
  isNewClient:
    versions:
      "1": "application.applicantData.previousApplications.firstaDate = null or abs(years and months duration(application.sysdate, application.applicantData.previousApplications.firstaDate)) < duration(\"P1M\")"
  age:
    versions:
      "1": "abs(years and months duration(application.applicantData.person.birthDate, application.sysdate)).years"
