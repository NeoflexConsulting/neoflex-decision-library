table:
  isNewClient:
    versions:
      "1": "application.applicantData.previousApplications.person.firstaDate = null or abs(years and months duration(application.sysdate, application.applicantData.previousApplications.preson.firstaDate)) < duration(\"P1M\")"
  age:
    versions:
      "1": "abs(years and months duration(application.person.birth, application.sysdate)).years"
